{
	"name": "starter",
	"version": "1.0.0",
	"description": "## How to run the program:",
	"main": "gulpfile.js",
	"scripts": {
		"installs": "npm ci --prefix client/ && npm ci --prefix server/",
		"start": "npm start --prefix client/ & npm start --prefix server/",
		"deploy": "npm run build --prefix client/ && gulp deploy",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"child_process": "^1.0.2",
		"gulp": "^4.0.2",
		"gulp-clean": "^0.4.0"
	}
}
